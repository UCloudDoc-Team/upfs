# FAQ

### 文件存储的使用有哪些限制吗？
文件存储对文件系统容量、文件最大长度、协议版本等方面有一定限制，请您参考[产品限制](/upfs/upfs_manual_instruction/limit)章节获取详细信息。

### 文件存储支持哪些访问协议?
UPFS目前支持POSIX与NFSv3两种协议。POSIX协议通过自研客户端实现，支持高性能场景，安装方式请参考[UPFS客户端安装](/upfs/upfs_guide/client_install.md)；NFS协议为标准协议，支持Linux与Windows系统挂载，详细操作请参考[Linux挂载](/upfs/upfs_guide/nfs_mount.md)和[Windows挂载](/upfs/upfs_guide/windows_mount.md)。

!> POSIX和NFS挂载方式各有适用场景，请根据业务需求选择。POSIX适用于对性能有极高要求的场景（如AI训练、渲染），NFS适用于需要标准化文件访问且兼容性要求高的场景。

### 文件存储是否允许跨地域访问？
UPFS不支持跨地域访问。

### 文件存储是否允许跨项目组访问？
UPFS文件系统目前仅允许同一项目组内的VPC进行访问，不支持跨项目组访问。请在创建文件系统前确认使用文件系统的云主机所在项目组。

### 单个文件系统允许访问的主机客户端数量是否受限？
客户端主机通过文件系统对应的挂载点进行访问，文件系统可挂载到同一项目下的云主机数目不做限制。

### 文件系统删除前需要做特殊操作吗？
是的，在文件系统被删除前，请首先确保该文件系统内数据不会再被使用，**UPFS文件系统删除后数据不可恢复**，删除操作请谨慎。在您删除文件系统前请确保所有使用该文件系统的主机上都对其做了卸载操作，否则在主机上残留的已删除文件系统实例会影响主机运行。

### 如何查看当前运行的客户端版本？
查看当前运行的upfs客户端进程的二进制文件   
```shell
ls -l /proc/`pidof upfs_client`/exe
```
如果有多个UPFS挂载，请按照upfs-*前缀的文件系统名筛选进程    

### 如何进行客户端版本的升级？
请参照[客户端升级](/upfs/upfs_guide/linux_mount_update.md)章节。

### POSIX和NFS协议如何选择？
两种协议适用场景不同：
- **POSIX协议**：通过UCloud自研客户端接入，性能更优，支持更丰富的挂载参数（如只读模式、禁止删除/重命名/覆盖等），适用于AI训练、渲染、EDA等对性能有极高要求的场景。需要预先安装客户端。
- **NFS协议**：标准协议，兼容性好，支持Linux和Windows系统，无需安装额外客户端（Linux需安装nfs-utils/nfs-common），适用于需要标准化文件访问、多平台共享数据的场景。

### NFS挂载有哪些限制？
- NFS协议仅支持Version 3版本。
- NFS挂载前需要在控制台上创建NFS挂载点（与POSIX挂载点不同）。
- Windows挂载需要安装NFS客户端并重启云主机。
- NFS挂载不支持POSIX挂载的高级参数（如forbidden_delete等）。

### NFS挂载地址与POSIX挂载地址是否相同？
不同。POSIX和NFS使用各自的挂载点地址，请在控制台文件系统详情页分别查看对应的挂载信息。